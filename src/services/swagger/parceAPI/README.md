# serviceserviceproto

Serviceserviceproto - JavaScript client for serviceserviceproto
No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
This SDK is automatically generated by the [Swagger Codegen](https://github.com/swagger-api/swagger-codegen) project:

- API version: version not set
- Package version: version not set
- Build package: io.swagger.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/),
please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install serviceserviceproto --save
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing 
into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

Finally, switch to the directory you want to use your serviceserviceproto from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

You should now be able to `require('serviceserviceproto')` in javascript files from the directory you ran the last 
command above from.

#### git
#
If the library is hosted at a git repository, e.g.
https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file, that's to say your javascript file where you actually 
use this library):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var Serviceserviceproto = require('serviceserviceproto');

var api = new Serviceserviceproto.ParcelServiceApi()

var body = new Serviceserviceproto.ParcelApiCreateParcelRequest(); // {ParcelApiCreateParcelRequest} 


var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.parcelServiceCreateParcelSize(body, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://localhost*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*Serviceserviceproto.ParcelServiceApi* | [**parcelServiceCreateParcelSize**](docs/ParcelServiceApi.md#parcelServiceCreateParcelSize) | **POST** /create_parcel | 
*Serviceserviceproto.ParcelServiceApi* | [**parcelServiceGetParcelAudit**](docs/ParcelServiceApi.md#parcelServiceGetParcelAudit) | **GET** /get_parcel_audit | 
*Serviceserviceproto.ParcelServiceApi* | [**parcelServiceGetParcels**](docs/ParcelServiceApi.md#parcelServiceGetParcels) | **GET** /get_parcels | 
*Serviceserviceproto.ParcelServiceApi* | [**parcelServiceSaveParcelSize**](docs/ParcelServiceApi.md#parcelServiceSaveParcelSize) | **POST** /save_parcel_size | 
*Serviceserviceproto.ParcelServiceApi* | [**parcelServiceUpdateParcel**](docs/ParcelServiceApi.md#parcelServiceUpdateParcel) | **POST** /update_parcel | 


## Documentation for Models

 - [Serviceserviceproto.ParcelApiAuditParcel](docs/ParcelApiAuditParcel.md)
 - [Serviceserviceproto.ParcelApiCreateParcelRequest](docs/ParcelApiCreateParcelRequest.md)
 - [Serviceserviceproto.ParcelApiCreateParcelResponce](docs/ParcelApiCreateParcelResponce.md)
 - [Serviceserviceproto.ParcelApiGetParcelAuditResponce](docs/ParcelApiGetParcelAuditResponce.md)
 - [Serviceserviceproto.ParcelApiGetParcelResponce](docs/ParcelApiGetParcelResponce.md)
 - [Serviceserviceproto.ParcelApiParcel](docs/ParcelApiParcel.md)
 - [Serviceserviceproto.ParcelApiSaveParcelSizeRequest](docs/ParcelApiSaveParcelSizeRequest.md)
 - [Serviceserviceproto.ParcelApiSaveParcelSizeResponce](docs/ParcelApiSaveParcelSizeResponce.md)
 - [Serviceserviceproto.ParcelApiSizes](docs/ParcelApiSizes.md)
 - [Serviceserviceproto.ParcelApiUpdateParcelRequest](docs/ParcelApiUpdateParcelRequest.md)
 - [Serviceserviceproto.ParcelApiUpdateParcelResponce](docs/ParcelApiUpdateParcelResponce.md)
 - [Serviceserviceproto.ProtobufAny](docs/ProtobufAny.md)
 - [Serviceserviceproto.RpcStatus](docs/RpcStatus.md)


## Documentation for Authorization

 All endpoints do not require authorization.

